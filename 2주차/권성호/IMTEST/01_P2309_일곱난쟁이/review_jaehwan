
		for(int i = 0; i<9; i++) {
			arr[i] = Integer.parseInt(br.readLine());
			sum += arr[i];
		}
    del = sum - 100;
    --> 9명 중 2명을 골랐을때 두명 합이 del이 되는 경우만 고려해야하는것 같아
    --> 난쟁이 한명이 del이 되는 경우를 고려하는건 오답에 가까운듯 
    --> 내꺼 리뷰 보고 추가로 다는것!
    --> del == arr[i]+arr[j] 이 구문 하나 만으로 하나의 난쟁이가 del을 초과하는 경우는 배제가능
        문제에서 난쟁이키가 자연수라 했으므로 하나의 난쟁이가 del 되더라도 나머지 한놈이 최소 1이 되서 del+1이기때문
     -> 그리고 이부분,
    -->for(int i=0; i<arr.length; i++) {
			    for(int j=0; j<i; j++) { 
      	    if(arr[i]!=del && arr[j]!=del && del == arr[i]+arr[j]) { 
    --> 이렇게 하면 arr[0]+arr[0] 경우도 나오지 않을까 똑같은 놈 2명 뽑는 케이스
    --> 나는 i=0,  j= i+1 이렇게 해서 중복 피했음
    
		// 찾으면 탈출 (여기서 궁금한게) break 한 개면 안쪽 for문 만 탈출하지 않나?? 바깥 for문도 한번에 탈출하는 방법이 없을까???
		
    -->나도 이거랑 같은 고민했었음 어제 -> 내 결론은 3중 4중 for문 이런거 아니면 그냥 냅두는게 맘편한거 같아 ㅋㅋㅋ 

  백준에서 오류나더라 이거 반례 있는거 같아
