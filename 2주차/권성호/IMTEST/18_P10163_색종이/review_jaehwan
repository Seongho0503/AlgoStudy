색종이마다 cnt 배열로 갯수를 체크함
그래서 중복될떄는 -1로 빼주고 중복되는 색종이 수를 늘렸음

나는 개인적으로 colp 배열을 0으로 초기화하고
색종이를 추가할때마다 colp 배열에 색종이 순서 값을 넣어주는게 좋은 것 같다

그러면 중복되는 것 고려 안해도 되고
색종이를 추가할때마다 그냥 값을 덮어쓰면 된다

만약에 첫번째 색종이를 1,1에 넣으면 colp[1][1]=1;
두번째 색종이를 1,1에 덮으면 colp[1][1]=2;

이런식으로 하면 좋을 것 같다 

나 300ms 성호 572ms

한번 고려해보기!
