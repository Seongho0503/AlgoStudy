나랑 로직이 똑같다
이게 아마 최적화 코드인 거 같다.

arr++ 단일 연산자를 사용하면 변수 초기화를 int arr = 0;을 꼭 해줘야 한다는 사실을 다시 복습하고 간다
나는 sum+=1;을 많이 쓰는데 int sum;까지만 해서 

x1 y1 x2 y2 점 위치로 가독성있게 좋다

//재환
if (map[i][j] != 1) {
						map[i][j] = 1;
						area+=1;
					}
//나 
if(map[row][col] != 1) sum += 1;
					map[row][col] = 1; // 1이여도 그냥 덮어쓰기인데 그냥 if문 안에 넣을걸... 계산만 더 수행하고 있네  
          
이 문제에서는 색종이마다 서로 가중치가 없어 (색칠 넓이가 그냥 1이여서) 두 코드 다 정상 작동한다 (if문 안에 있든 없든)
